import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Origem local usada para testar o projeto em outros dispositivos.
  allowedDevOrigins: ["192.168.3.97"],
};

export default nextConfig;
